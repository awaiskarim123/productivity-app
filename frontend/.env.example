# Copy to .env — these apply to the whole app (api-config, api client, auth).
# Local:  cp .env.example .env
# Vercel: set in Project → Settings → Environment Variables

# Backend API base (includes /api). Required in production.
VITE_API_URL="http://localhost:4000/api"

# Frontend site URL. Optional; production defaults to https://productivity-app-dusky.vercel.app
# VITE_SITE_URL="https://productivity-app-dusky.vercel.app"
