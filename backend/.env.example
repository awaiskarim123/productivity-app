DATABASE_URL="postgresql://postgres:postgres@localhost:5432/productivity_app?schema=public"
JWT_ACCESS_TOKEN_TTL="15m"
JWT_REFRESH_TOKEN_TTL="30d"
JWT_SECRET="replace-with-strong-secret"
PORT="4000"
# Rate limiting: max requests per IP per window (default 100 per 15 min)
RATE_LIMIT_MAX=100
RATE_LIMIT_WINDOW_MS=900000
